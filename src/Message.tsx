// PascalCasing
function Message() {
  // JSX: JavaScript XML
  const name = "Gearoid";
  return <h1>Hello {name}</h1>;
}

// Export so that the component can be used elsewhere (Imported)
export default Message;
